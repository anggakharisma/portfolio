{
  "devDependencies": {
    "cssnano": "^4.1.10"
  },
  "scripts": {
    "buildit": "parcel build index.html --no-source-maps",
    "deploy": "gh-pages -d dist --git git",
    "clean": "gh-pages clean"
  },
  "dependencies": {
    "gh-pages": "^2.1.0"
  }
}